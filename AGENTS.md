# Agent memory (nota)

## Workspace and stack

- **Monorepo:** [`pnpm-workspace.yaml`](pnpm-workspace.yaml) lists `apps/*`, `packages/*`: `@getmadrid/clerk-oauth-protocol` (desktop OAuth URLs; dual Vite/Node export), `@getmadrid/database-types`, `@getmadrid/validation` (Zod), `@getmadrid/internal-note-link` (`/notes/:uuid` path helpers; shared w/ native), `@getmadrid/isomorphic-helpers` (plain Node/browser utils—`debounce`; use it, don't hand-roll `setTimeout` debounce/throttle), `@getmadrid/note-link-graph` (pure link graph + `notesToIdMap`), `@getmadrid/notes-offline-core` (merge/outbox ordering + types; no storage), `@getmadrid/notes-offline` (IndexedDB adapter; [`src/lib/notes-offline.ts`](apps/nota/src/lib/notes-offline.ts) re-exports + [`notes-offline-sync.ts`](apps/nota/src/lib/notes-offline-sync.ts) Supabase drain), `@getmadrid/design` (Base UI + shadcn-style primitives in [`packages/design`](packages/design); **subpath imports only**—e.g. `@getmadrid/design/button`, `@getmadrid/design/utils`—no root export; shadcn [`components.json`](apps/nota/components.json) targets it; shell/routes/TipTap import `@getmadrid/design/*` subpaths directly (`button`, `tooltip`, `card`, `spinner`, `utils`) in [`apps/nota`](apps/nota) (context/Tailwind).
- **Storybook (`@getmadrid/design`):** `pnpm exec nx storybook @getmadrid/design` (build: `build-storybook`); config in [`packages/design/.storybook`](packages/design/.storybook) — own vite (not `vite.config.mts`, which is a `build.lib` config) + `@tailwindcss/vite`. Stories live beside components as `*.stories.tsx`. Theme tokens are **`@getmadrid/design/theme-tokens.css`** ([`src/theme-tokens.css`](packages/design/src/theme-tokens.css)) — the shadcn palette + `@theme inline` map, imported by both [`apps/nota/styles.css`](apps/nota/styles.css) and the Storybook preview; do not redefine `--primary`/`--background` per app. Toolbar light/dark stamps `html.light`/`html.dark`.
- **Primary app** [`apps/nota`](apps/nota): Vite SPA `index.html` → `src/main.tsx` → `NotaApp`, React 19, Nx; `cn` from [`src/lib/utils.ts`](apps/nota/src/lib/utils.ts) re-exports `@getmadrid/design/utils`; theme tokens (`bg-background`, `muted`, `foreground`)—no hard-coded light neutrals on notes. Server halves live in [`src/app/api/*`](apps/nota/src/app/api) + [`src/server/`](apps/nota/src/server) (absorbed from the deleted `nota-server`); the client tree must stay free of `@clerk/backend` and `*.server` imports — guarded by [`nota-app-client-boundary.spec.ts`](apps/nota/src/nota-app-client-boundary.spec.ts).
- **Client root** [`apps/nota/src/`](apps/nota/src/); Vite/tsconfig `~/` + `@/` there (not top-level `app/`). Composition: [`app-root.tsx`](apps/nota/src/app-root.tsx), notes chrome in [`@getmadrid/notes-chrome-ui`](packages/notes-chrome-ui); create/delete [`clientCreateNote`](apps/nota/src/lib/create-note-client.ts) / [`clientDeleteNoteById`](apps/nota/src/lib/delete-note-client.ts).
- **Data:** Supabase Postgres RLS; Clerk JWT third-party; `@supabase/supabase-js` + `accessToken` from Clerk. Typed access [`src/models/notes.ts`](apps/nota/src/models/notes.ts)—no ad hoc UI queries.
- **TipTap:** client-only; body ProseMirror JSON `jsonb`. Sync compares `content` w/ `Node.fromJSON` + `doc.eq` (not `JSON.stringify`). After `updateNote`: merge latest local body into note for `onNoteUpdated`—`.select()` row can lag (typing during save, title before body debounce).
- **Env (app):** `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` ([`.env.example`](apps/nota/.env.example)). Server-only in the **same** app: **`CLERK_SECRET_KEY`** (Clerk Billing + route auth), **`SUPABASE_SECRET_KEY`** (`sb_secret_…`; deprecated `SUPABASE_SERVICE_ROLE_KEY` read as fallback). All API routes are **same-origin** `src/app/api/*` authenticated by the Clerk session cookie via `clerkMiddleware` — no base URL, no Bearer. Never expose `CLERK_SECRET_KEY` / Supabase service keys client-side. SQL [`supabase/migrations/`](supabase/migrations/) incl. [`0008_clerk_third_party_auth.sql`](supabase/migrations/0008_clerk_third_party_auth.sql).
- Root [`.gitignore`](.gitignore): include `supabase/.temp/` (CLI metadata may hold connection strings).
- **Stack discipline:** Nx + Vite + React + Astro marketing—no new framework / major bump w/o **user-visible** gain or **security** (say in PR).

## Package manager and dependency catalogue

- **Toolchain versions:** nx **23.1.1** (all `@nx/*` + catalog entries pinned to it — bump via `nx migrate`, never one plugin alone), TypeScript **7.0.2** native `tsc` + **6.x** JS API shim (see TypeScript section), Vite **8** (Rolldown; `build.rolldownOptions`, not `rollupOptions`; transforms run through **Oxc** so vite/vitest configs use `oxc: { jsx: … }`, not `esbuild:`), `@vitejs/plugin-react` **6** (no `babel` option), Vitest **4**. React pinned **19.2.3** in catalog + `pnpm.overrides` (was aligned to the Expo SDK; the pin stayed after the mobile app was removed).
- **pnpm only** at repo root ([`package.json`](package.json) **`packageManager`**). Use **`pnpm install`**, **`pnpm exec nx …`**, **`pnpm add -wD …`**. No **`package-lock.json`** / **`npm ci`** here. CI, Vercel, Docker use **`pnpm install --frozen-lockfile`** (with **`corepack enable pnpm`** where needed). **`storeDir`** is **repo-local** ([`pnpm-workspace.yaml`](pnpm-workspace.yaml) → **`.pnpm-store/`**, gitignored); disk grows w/ deps like any pnpm store.
- **Catalogue:** Shared versions for external deps in **>1** of **`packages/*`** or **`apps/*`** live under **`catalog`** in [`pnpm-workspace.yaml`](pnpm-workspace.yaml); each consumer refs **`"catalog:"`** in **`package.json`** (not divergent semver). Internal **`@getmadrid/*`** use **`workspace:*`**.
- **Hoisting:** [`.npmrc`](.npmrc) **`node-linker=hoisted`** keeps **`node_modules`** compatible w/ **electron-builder** (see [`apps/nota-electron/electron-builder.yml`](apps/nota-electron/electron-builder.yml)).

- **Dev:** **`pnpm run dev`** = `nx run-many -t dev`—parallel (notes **http://localhost:3000**, marketing, Electron). Single app: **`pnpm exec nx dev <project>`** (e.g. **`pnpm exec nx dev @getmadrid/nota`**). Electron-only: **`pnpm run electron:dev`** (`nx dev nota-electron`).
- **Git hooks:** Root [`package.json`](package.json) **`postinstall`** runs **`husky`**; [`.husky/pre-commit`](.husky/pre-commit) runs **`pnpm exec lint-staged --relative`** via Nx CLI (`nx exec -- eslint --fix` + `nx format:write --files`). **`pnpm run format`** runs `nx format:write`.

## Deployment and desktop releases

- **Vercel:** Root Dir `apps/nota`. [`vercel.json`](apps/nota/vercel.json) is Next-only: `framework: nextjs`, `pnpm install --frozen-lockfile` from repo root, `pnpm exec nx build @getmadrid/nota` (→ `next build`, output `.next`). **No `outputDirectory` / SPA rewrites / headers there**—the App Router serves routing, and CSP + `X-Content-Type-Options` + `Referrer-Policy` live in [`next.config.ts`](apps/nota/next.config.ts) `headers()` (dev relaxes `script-src`/`connect-src` for Turbopack HMR). Mirror env from [`.env.example`](apps/nota/.env.example), **including the server-only secrets** (`CLERK_SECRET_KEY`, `SUPABASE_SECRET_KEY`) — the Next app serves the API routes itself, so there is no separate API deployment. Marketing ([`apps/nota-marketing`](apps/nota-marketing)) still ships those headers from its own config.
- **CI:** [`.github/workflows/lint.yml`](.github/workflows/lint.yml) (`pnpm exec nx run-many -t lint --outputStyle=static`), [`.github/workflows/test.yml`](.github/workflows/test.yml) (`pnpm exec nx run-many -t test --outputStyle=static`; no push/PR build job—Vercel + Electron release still build), [`.github/workflows/audit.yml`](.github/workflows/audit.yml) (`pnpm audit --audit-level moderate`). [`.github/dependabot.yml`](.github/dependabot.yml)—weekly dep PRs (`package-ecosystem: npm` updates `pnpm-lock.yaml`).
- **Electron release:** **`pnpm exec nx run @getmadrid/nota-electron:electron:release`** / **`pnpm run release:electron`** → Electron shell build then [`tools/electron-github-release.mjs`](tools/electron-github-release.mjs) (`electron-builder --publish always`). Does **not** build `@getmadrid/nota` / Next. **`GH_TOKEN`** / **`GITHUB_TOKEN`**. [`.github/workflows/release-electron.yml`](.github/workflows/release-electron.yml): `v*` or `workflow_dispatch`; prerelease tag → **`EP_DRAFT`**. [`electron-updater`](apps/nota-electron/src/nota-updater.ts) when packaged (GitHub Releases feed; Settings → **Check for updates** + **Restart and update** via preload IPC); assets need **ZIP** + **`latest-mac.yml`**. ESM main: **`import electronUpdater from 'electron-updater'`** then `autoUpdater`—named import from CJS breaks. Pin **`electronVersion`** in [`electron-builder.yml`](apps/nota-electron/electron-builder.yml); **`npmRebuild: false`** (hoisted `node_modules`). Workflow **`macos`**: **`environment: Production`**. **`CSC_LINK`** only if cert present. Docs [`apps/nota-electron/README.md`](apps/nota-electron/README.md).
- **Packaged Electron:** [`electron-builder.yml`](apps/nota-electron/electron-builder.yml) packages the Electron shell only (no `nota/dist` embed); loads **`https://app.getmadrid.app`** ([`app-load-url.ts`](apps/nota-electron/src/app-load-url.ts)). Client env for the UI comes from Vercel, not the Electron release job. Dev Electron waits local Next (`http://localhost:3000`). macOS Dock uses **`buildResources/icon.png`** (light) + **`icon-dark.png`** (from [`icon-dark.svg`](apps/nota-electron/buildResources/icon-dark.svg)) via **`nativeTheme`** in [`main.ts`](apps/nota-electron/src/main.ts); bundle **`.icns`** stays light mark.

## TypeScript and generated types

- **TS 7 dual install:** catalog [`pnpm-workspace.yaml`](pnpm-workspace.yaml) `typescript` = `npm:@typescript/typescript6@6.0.2` (re-exports JS API **6.0.3**; `tsc6`); `@typescript/native` = `npm:typescript@7.0.2` (`tsc`). Root [`package.json`](package.json) depends on both. **7.0 has no programmatic API** — `typescript-eslint`, `vite-plugin-dts`, `ts-node` import `typescript` and must stay on the 6 shim until **7.1**. Never point the `typescript` catalog name at 7 alone. `pnpm exec tsc` = 7; `pnpm exec tsc6` = 6 (compare). No `ignoreDeprecations`. Base [`tsconfig.base.json`](tsconfig.base.json): `esModuleInterop` **true**, `stableTypeOrdering` **true**.
- [`apps/nota/tsconfig.app.json`](apps/nota/tsconfig.app.json): `rootDir` `.`, includes `src/**` only—files outside break imports.
- Generated types [`packages/database-types/src/lib/database.types.ts`](packages/database-types/src/lib/database.types.ts); app re-export [`apps/nota/src/types/database.types.ts`](apps/nota/src/types/database.types.ts) for `~/types/database.types`. Run `supabase gen types` into package; keep re-export.

## Git commits (enforced)

Must use [Conventional Commits](https://www.conventionalcommits.org/).

- Subject: `<type>(optional scope): description`—types `feat|fix|docs|style|refactor|perf|test|build|ci|chore|…`; imperative; ~72 chars; lowercase after colon unless proper noun.
- Body when useful; **`BREAKING CHANGE:`** footer if incompatible.
- Reject vague subjects (`update`, `fixes`, `wip`, …); unrelated changes → split commits or scoped `chore`. Committing for user: **stage by path** if mixed.
- **Commit straight onto `main`** — no feature branch unless the user asks for one.

## Clerk + Supabase client

- Supabase **Authentication → Third-party** Clerk; `createClient(url, anonKey, { accessToken: () => getToken() })`. **`CLERK_SECRET_KEY`** server-side only, in [`apps/nota/src/server/`](apps/nota/src/server) + route handlers.

## Notes UI and client state

- [`NotesChrome`](packages/notes-chrome-ui/src/notes-chrome.tsx); path navigation via App Router + [`app-navigation`](packages/app-navigation-core)—not nested React Router. Sidebar Zustand persist `nota-notes-sidebar`.
- `/notes`: `h-dvh`, `min-h-0`; only `main` scrolls; sidebar list `flex-1 min-h-0 overflow-y-auto`. Electron `titleBarStyle: 'hiddenInset'`: [`useIsElectron`](apps/nota/src/lib/use-is-electron.ts) → `pl-20`, safe-area; collapsed expand strip `min-h-[52px]` fixed; [`-webkit-app-region`](apps/nota/src/styles.css): `.electron-window-drag` / `.electron-window-no-drag`, [`ElectronWindowDragBand`](apps/nota/src/components/electron-window-drag-band.tsx) in [`app-root.tsx`](apps/nota/src/app-root.tsx). No global top nav; Graph + Settings footer [`routes/notes.tsx`](apps/nota/src/routes/notes.tsx). Sticky doc title [`sticky-doc-title.tsx`](apps/nota/src/context/sticky-doc-title.tsx) + `IntersectionObserver`. **`useStickyDocTitle`** must not throw w/o provider (Vitest stubs). Serif sidebar/empty-state headings; open note title sans. Editor: debounced title textarea; empty label [`note-title.ts`](apps/nota/src/lib/note-title.ts) matches **"Untitled Note"**; body autosave touches **`content`** only. After saves: **`refreshNotesList`** / context. Delete via sidebar + palette—**not** by doc title—[`clientDeleteNoteById`](apps/nota/src/lib/delete-note-client.ts). Create [`clientCreateNote`](apps/nota/src/lib/create-note-client.ts). Palette [`command-palette.tsx`](apps/nota/src/components/command-palette.tsx) from [`signed-in-command-palette.tsx`](apps/nota/src/signed-in-command-palette.tsx); Cmd/Ctrl+K; Clerk sign-out + hash—not `Form` posts. GSAP [`nota-motion.ts`](apps/nota/src/lib/nota-motion.ts); palette GSAP + Base UI Dialog `preventUnmountOnClose`; sidebar tween `maxWidth`/`opacity`. **`vite.config.mts`**: `ssr.noExternal: ['gsap', '@gsap/react']`. No GSAP on TipTap surface.

## Auth forms

- Zod validation [`src/lib/validation/auth.ts`](apps/nota/src/lib/validation/auth.ts).

## Testing

- **Unit tests** use **Arrange–Act–Assert (AAA)**; mark w/ `// Arrange|Act|Assert`—[`.cursor/rules/aaa-testing-pattern.mdc`](.cursor/rules/aaa-testing-pattern.mdc). Vitest [`vitest.setup.ts`](apps/nota/vitest.setup.ts); colocate `*.spec.ts(x)` under `src/`. Server-lib specs are Vitest beside the source in [`apps/nota/src/server/`](apps/nota/src/server).
- **E2E tests** use **Given–When–Then**: **Given** initial context, **When** action, **Then** assert outcome (structure scenarios/steps so).

## Learned User Preferences

- Folder sidebar tint: selection shows on folder icon + name only—no row background fill (tint applies to icon/name per light/dark tone). Sidebar must visually distinguish folders vs notes (icons), not rely on text alone.
- Micro-interactions: UI feels responsive/alive; preserve typing/editing focus (no motion on TipTap surface). North star: native Swift/macOS (Apple Design)—AppKit Notes + Spotlight on desktop Mac, not iOS sheets/swipes. Keyboard-initiated UI (palette Mod+K, Mod+D, history, create) stays instant; pointer opens/nav may use short motion (≤180ms). Asymmetric press on chrome (fast press-in, slower release).
- Customer-facing copy (marketing, blog, welcome note, i18n, in-app labels/empty states/paywall, share/OG titles): always invoke **`unslop`** before writing or editing. Read the skill, draft, then run its scan/rewrite/self-audit. Do not ship those strings without that pass.
- Marketing/blog copy: no em-dashes (use commas, semicolons, colons); author byline **Leonardo**; bold `font-weight: 500`; match [mrlemoos.dev](https://mrlemoos.dev) voice.
- PDFs inline TipTap `notePdf` at cursor. Doc title textarea: heavy sans + `text-pretty`. `@` mentions for internal links—no toolbar. Minimal attachment chrome—drag/drop; show errors. After `@` insert: `setParagraph` + clear stored marks. Copy: calm voice; no generic AI/editor prose; optional **assistive capture** (recording → `noteAudio` + blocks / append; offline `append` on jobs)—`POST /api/audio-to-note`+xAI, not ghostwriting. **No unpaid tier / free trial** in public copy—Mac app, subscribe in-app; Settings = **PricingTable** checkout not essays. Offline-first local capture. Mod+D today's note: no gamification; daily title = local long date ([`todays-note.ts`](apps/nota/src/lib/todays-note.ts)), not Untitled. `Button` Base UI—no `asChild`; links = `<a>` + `buttonVariants` or `Button`+`window.open`. **NotaLogo** ↔ `public/favicon.svg`; geometry change → **`pnpm run generate:nota-icons`** (favicon, icns, light/dark). App/Dock mark: macOS 26 glass, not Notion-like; Dock must use the rounded mask, not a square glyph. Packaged Mac: care about first-open latency (hosted app). W/ **system** theme, UI follows OS appearance incl. macOS **auto** light/dark (time-of-day), not only first paint.
- **Madrid logo language:** quiet aristocracy, not a civic souvenir. The full mark is a shallow baroque arch around an engraved `M`, plus a provisional lower-case `madrid` wordmark in a Didot-adjacent serif. Draw from Madrid wrought iron with one flourish; show royalty through proportion, near-black and warm bone, and restrained engraving. Dock/favicon use the same adaptive rounded-square plate. Never add crowns, bears, strawberries, flags, crests, faux gold, cursive, or cathedral detail.
- Long notes: if user scrolls away from caret (e.g. after typing at bottom), don't programmatically scroll/refocus back to caret w/o new typing—that's unwanted churn.
- Notes chrome: Claude-style glass is reference—sidebar as darker, stronger-blur rail than main; on packaged macOS Electron app root must stay transparent where notes run or native vibrancy reads as solid sheet. No “Notes” heading at the top of the sidebar; collapse/expand control has a tooltip. Collapsed rail peeks only while the pointer is over that strip. Electron: closed sidebar hides traffic lights; hover-reveal on the invisible top-left zone stays; with a banner, faint outlines mark the lights.
- Note banners: revisiting a note must not re-flash/reload the banner as if unloaded; sidebar note titles stay readable when a banner note is open (enough contrast over banner chrome).
- Chrome/editor UI: prefer `@getmadrid/design` primitives—`Icon` / itsHover-style animated icons (`@getmadrid/design/icons`), Dialog (`@getmadrid/design/dialog`; add Base UI/shadcn-style if missing)—over inline SVG or bespoke overlays in packages like `notes-chrome-ui` / `note-editor-ui`.
- **Naming:** the word **shell** is banned in **component names** and **filenames** (e.g. no `NotesShell`, `notes-shell.tsx`, `*-shell-*`). Prefer **chrome** for the notes workspace UI (`NotesChrome`, `notes-chrome.tsx`, `notes-chrome-parts.tsx`, `@getmadrid/notes-chrome-core/notes-chrome`, `useNotesSidebarMotion`). Unrelated senses stay (Electron “shell”, `shell.openExternal`, spring preset `shell`, local vars).

## Learned Workspace Facts

- **[`NotesDataProvider`](apps/nota/src/context/notes-data-context.tsx):** [`fetchNotaProEntitled`](apps/nota/src/lib/nota-server-client.ts) → same-origin `GET /api/nota-pro-entitled`, Clerk cookie auth. **`notaProEntitled`:** true = Supabase notes + `user_preferences` + IndexedDB merge [`notes-offline/`](apps/nota/src/lib/notes-offline/); false = empty list (no unpaid IndexedDB bootstrap). Offline: **`readNotaServerEntitledSession`**. Unpaid signed-in → paywall, hash → Settings. **`NoteDetailPanel`** / uploads / cloud save need entitlement. **`editor_settings`** jsonb sync when entitled online. Empty vault + `welcome_seeded` false → [`runWelcomeNoteSeedIfNeeded`](apps/nota/src/lib/welcome-note-seed.ts) `welcome-note-doc`, **`upsertUserPreferences`**, then **`refreshNotesList`** + **`setAppHash`**. Clerk Billing [`nota-pro-entitlement.ts`](apps/nota/src/server/nota-pro-entitlement.ts); Settings **`PricingTable`**; post-purchase **`postNotaProInvalidate`** + **`refreshNotesList`**. **`pnpm exec nx run @getmadrid/nota:validate-billing`** w/ `CLERK_SECRET_KEY` in [`apps/nota/.env`](apps/nota/.env). **`refreshNotesList({ silent: true })`**—no global `loading`/`chromeReady` flip; reset sequencing on **`userId`** change.
- **Attachments:** bucket `note-pdfs`; [`notePdf`](apps/nota/src/components/tiptap/) / [`noteImage`](apps/nota/src/components/tiptap/) atoms; [`pdf-attachment-client.ts`](apps/nota/src/lib/pdf-attachment-client.ts). DnD needs entitlement. Rename filename RLS. Images: signed URLs by stable `storage_path`. Banner chrome [`note-banner-chrome.ts`](apps/nota/src/lib/note-banner-chrome.ts); signed URL cache [`note-attachment-signed-url-cache.ts`](apps/nota/src/lib/note-attachment-signed-url-cache.ts) so revisit does not re-fetch/flash. PDF preview lazy [`pdf-js-modal-preview.tsx`](apps/nota/src/components/pdf-js-modal-preview.tsx); lazy-load PDF.js. Delete note: [`deleteNote`](apps/nota/src/models/notes.ts) clears storage then row; CASCADE attachments.
- **Link previews:** same-origin [`GET /api/og-preview?url=`](apps/nota/src/app/api/og-preview/route.ts); client [`og-preview-client.ts`](apps/nota/src/lib/og-preview-client.ts); server [`og-preview.server.ts`](apps/nota/src/server/og-preview.server.ts). TipTap `linkPreview`; paragraph promotion in [`packages/editor/src/components/tiptap/link-preview-scan.ts`](packages/editor/src/components/tiptap/link-preview-scan.ts) (`convertLinkOnlyParagraphs`)—only when trimmed anchor text = trimmed `href`, paragraph not under `listItem`/`taskItem` (tables + other blocks allowed). Vitest [`apps/nota/src/components/link-preview-scan.spec.ts`](apps/nota/src/components/link-preview-scan.spec.ts). Fail → paragraph + link; `skipLinkPreview` stops loops. Internal `/notes/<uuid>` links not promoted. `fetchOgPreview`: `redirect: 'manual'`, per-hop safety, `dns.lookup`; `og:image` http(s) only; client [`safeOgImageSrcForPreview`](apps/nota/src/lib/og-image-url.ts).
- **Internal links:** path `/notes/<uuid>` — canonical helpers [`@getmadrid/internal-note-link`](packages/internal-note-link); app re-export [`internal-note-link.ts`](apps/nota/src/lib/internal-note-link.ts); `NotaLink` custom click. Graph UI [`@getmadrid/note-graph`](packages/note-graph) (`NotesGraphView`, hover edges, specs); pure math [`@getmadrid/note-link-graph`](packages/note-link-graph) (`buildNoteLinkGraph`, `extractOutgoingNoteIdsFromContent`, `notesToIdMap`). [`filterNotesForNoteGraph`](apps/nota/src/lib/note-editor-settings.ts)—`editor_settings.showInNoteGraph`; [`note-layout-menu.tsx`](apps/nota/src/components/note-layout-menu.tsx) toggle.
- **TipTap sync (incl. mermaid):** **Yjs local-first**—editor binds a per-note Y.Doc ([`@getmadrid/notes-yjs-core`](packages/notes-yjs-core) + IndexedDB); derived `content` jsonb is the read-model. Never `setContent`/`flushSync` inside a React layout/effect—use [`scheduleEditorSetContent`](packages/editor/src/lib/schedule-editor-set-content.ts). `setContent` on **`noteId`** change, not every refresh for same note—except assistive inject: bump **`contentRevision`** from row (`updated_at`); merge list row locally; refetch **`note_attachments`** for `noteAudio`. **`lastSavedContent`** = merged local after save. Stabilise `extensions` `useMemo` deps (e.g. `[placeholder]` only in [`tiptap-editor.tsx`](packages/editor/src/components/tiptap-editor.tsx)). `@` confirm: **`view.dispatch`** + `setStoredMarks([])` in [`tryConfirmNoteMention`](apps/nota/src/components/tiptap-note-mention-flow.ts); NumpadEnter = Enter; don't `preventDefault` on confirm w/o insert. **Typewriter scroll:** [`alignTypewriterScroll`](apps/nota/src/components/note-editor.tsx) respects [`nota-typewriter-scroll-guard`](apps/nota/src/lib/nota-typewriter-scroll-guard.ts)—after manual scroll on note scroll root, skip caret alignment until `beforeinput`/paste/pointerdown on body, so background `docChanged` (link preview updates, `convertLinkOnlyParagraphs` in [`tiptap-editor.tsx`](packages/editor/src/components/tiptap-editor.tsx)) doesn't snap viewport back to caret. **Mermaid:** [`NotaCodeBlock`](apps/nota/src/components/tiptap/nota-code-block.tsx); `StarterKit` w/o default codeBlock; [`nota-lowlight.ts`](apps/nota/src/lib/nota-lowlight.ts); mermaid lazy, `securityLevel: 'strict'`, theme from `html.dark`.
- **Palette + tables + emoji:** Cmd/Ctrl+N create if entitled; `data-nota-command-palette` blocks Mod+D; tables in [`tiptap-editor.tsx`](apps/nota/src/components/tiptap-editor.tsx) + [`table-editor-menu.tsx`](apps/nota/src/components/tiptap/table-editor-menu.tsx); mousedown capture on table controls. Emoji: `@tiptap/extension-emoji` `^2.27.2`, `enableEmoticons: false`, `suggestion.allow: () => false`. Canvas stub in vitest for emoji spec.
- **Modularize SPA libs (ADR 0002) + Next path routing (ADR 0003):** layer `data-source` → `note-runtime` → feature `-core`/`-ui` → `apps/nota` composition. Notes chrome in [`@getmadrid/notes-chrome-ui`](packages/notes-chrome-ui) ([`notes-chrome.tsx`](packages/notes-chrome-ui/src/notes-chrome.tsx)); motion in `@getmadrid/nota-motion-{core,ui}`; journal/capture/palette/folders/navigation/electron/writing-activity/editor as matching pairs. **Packaging:** modularize pkgs use **subpath `exports` → `src`** + `@getmadrid/source` condition and `tsc` emitDeclarationOnly (like `note-link-graph`)—not Vite `lib.entry` → `dist/*.js` (`design`/`helper-hooks` style). App Vite/Next `conditions: ['@getmadrid/source', …]` compiles TS; do not “fix” exports to missing `dist` JS. Resizable sidebar rubber-band via `@getmadrid/nota-motion-core`; inactive panels `hidden`+`inert`. **Motion contract:** keyboard → `data-motion="instant"`; pointer fades + **`data-nav-intent`**; spring/press in **`@getmadrid/design/motion-tokens`**. **Journal** `/notes/journal` via `@getmadrid/note-journal-{core,ui}`. Bootstrap: App Router + [`providers.tsx`](apps/nota/src/providers.tsx) (`ClerkProvider` path routing, `signInForceRedirectUrl="/notes"`, `nota:` deep links). Signed-in vault is a client shell (do not SSR note bodies). Share cards: `/s/[token]` server HTML, `og:title` `{author} shared {title}`, document title `{title}–Madrid`; Electron still loads the hosted origin. Not entitled → Settings; unknown path → `/404`; missing note → notFound. Theme = **`@getmadrid/design/theme`**; **`system`** must subscribe to `prefers-color-scheme` changes. Electron: sidebar toggle above drag band (`electron-window-no-drag`).
- **Prefs:** `user_preferences` RLS; entitled → cloud sync toggles. `dailyNoteIdByLocalDate` local Zustand. [`openTodaysNoteClient`](packages/app-navigation-ui/src/open-todays-note.ts) no-op if not entitled. History Mod+[ / Mod+] [`use-notes-history-shortcut.ts`](packages/app-navigation-ui/src/use-notes-history-shortcut.ts)—skip when palette focused.
- **Marketing [`apps/nota-marketing`](apps/nota-marketing):** Astro; [`BaseLayout.astro`](apps/nota-marketing/src/layouts/BaseLayout.astro) Analytics + optional Clarity **`VITE_CLARITY_PROJECT_ID`**. [`astro.config.mjs`](apps/nota-marketing/astro.config.mjs) `vite.envPrefix`. Consumes **`@getmadrid/note-doc-plain-text`** package (not an app-path alias). Refunds [`refunds.astro`](apps/nota-marketing/src/pages/refunds.astro). **`pnpm run generate:og`** in that app for default OG image.
- **Electron:** translucency `transparent: true`; macOS `vibrancy`; `html.nota-electron` CSS unlayered for transparency. Notes route: transparent [`app-root.tsx`](apps/nota/src/app-root.tsx) + `html.nota-electron #root` so vibrancy not covered by opaque `bg-background`; chrome classes from [`@getmadrid/notes-chrome-core/notes-chrome`](packages/notes-chrome-core/src/notes-chrome.ts) (`notesSidebarChrome` / `notesMainChrome`, `prefers-reduced-transparency` fallbacks in [`styles.css`](apps/nota/styles.css)). **`app.setName('Madrid')`** early in [`main.ts`](apps/nota-electron/src/main.ts) so `electron-updater` default notifications use **Madrid**, not npm `package.json` `name`. **`main.ts`** `setWindowOpenHandler` → `shell.openExternal`. Packaged load calls Clerk FAPI on custom domain—patch missing CORS via [`clerk-fapi-cors.ts`](apps/nota-electron/src/clerk-fapi-cors.ts); Clerk production DNS **DNS only** (not Cloudflare-proxied). Release workflow injects `VITE_*` from secrets; empty `VITE_*` breaks packaged build—**`--skip-nx-cache`** web build when env changes.
- **Hosted SPA pitfalls:** Fontsource import from [`main.tsx`](apps/nota/src/main.tsx) not only PostCSS `@import` (missing woff2). Safari theme chrome: paint hex **`--background-hex`** on `body`/sticky surfaces via [`theme-chrome.css`](packages/design/src/theme-chrome.css)—`oklch()` in `theme-color` meta not honoured. CSP: Clerk Frontend API origin, PostHog `https://*.i.posthog.com`, `worker-src 'self' blob:`; align duplicate CSP headers. Custom Clerk domain → add **`script-src`**. Don't alias **`@clerk/shared`** broadly. xAI STT: only **WAV**/**MP3**—transcode WebM/Ogg/M4A to PCM WAV. Icons: `Icon` from `@getmadrid/design/icon` over itsHover set `@getmadrid/design/icons` (app + marketing; subpath imports only). Agent config: [`.agents`](.agents) is canonical; [`.claude`](.claude) / [`.cursor`](.cursor) / [`.opencode`](.opencode) symlink to it; [`CLAUDE.md`](CLAUDE.md) → [`AGENTS.md`](AGENTS.md).
- **i18n (translations):** All user-facing labels translated to all supported locales in [`packages/i18n/src/lib/i18n.ts`](packages/i18n/src/lib/i18n.ts). Use `t("British English key")` w/ optional placeholder support (e.g. `t("Message {count}", { count: 5 })`). `@getmadrid/i18n` exports `SupportedLocale` (`'en-GB' | 'es-ES' | 'pt-BR'`), `LOCALE_OPTIONS`, `createTranslator`. New UI labels must be added to `TRANSLATIONS` dict for all non-en-GB locales.

<!-- nx configuration start-->
<!-- Leave the start & end comments to automatically receive updates. -->

## General Guidelines for working with Nx

- For navigating/exploring the workspace, invoke the `nx-workspace` skill first - it has patterns for querying projects, targets, and dependencies
- When running tasks (for example build, lint, test, e2e, etc.), always prefer running the task through `nx` (i.e. `nx run`, `nx run-many`, `nx affected`) instead of using the underlying tooling directly
- Prefix nx commands with the workspace's package manager (e.g., `pnpm nx build`, `npm exec nx test`) - avoids using globally installed CLI
- You have access to the Nx MCP server and its tools, use them to help the user
- For Nx plugin best practices, check `node_modules/@nx/<plugin>/PLUGIN.md`. Not all plugins have this file - proceed without it if unavailable.
- NEVER guess CLI flags - always check nx_docs or `--help` first when unsure

## Scaffolding & Generators

- For scaffolding tasks (creating apps, libs, project structure, setup), ALWAYS invoke the `nx-generate` skill FIRST before exploring or calling MCP tools

## When to use nx_docs

- USE for: advanced config options, unfamiliar flags, migration guides, plugin configuration, edge cases
- DON'T USE for: basic generator syntax (`nx g @nx/react:app`), standard commands, things you already know
- The `nx-generate` skill handles generator discovery internally - don't call nx_docs just to look up generator syntax

<!-- nx configuration end-->

## Agent skills

### Issue tracker

Local markdown under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles w/ default strings. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context monorepo via `CONTEXT-MAP.md`. See `docs/agents/domain.md`.
