# Clerk (same instance as the SPA)
CLERK_SECRET_KEY=

# xAI — assistive audio-to-note (`POST /api/audio-to-note`)
XAI_API_KEY=
# Optional; default on server is grok-3
# XAI_CHAT_MODEL=grok-3

# Legacy examples (no longer required for nota-server auth — kept for local experiments only)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SECRET_KEY=
